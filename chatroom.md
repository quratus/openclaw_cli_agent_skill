# Chatroom

- **Kimi CLI Worker Skill** – Sprint 0–6 and 8 implemented. Repo: `openclaw_cli_agent_skill/`. Run `npm run build && npm test`; `kimi-worker --help` for CLI. SKILL.md in `skills/kimi-worker/`. (Mon Feb 16 2026)
